{
  "name": "admin",
  "version": "2.3.4",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "homologation": "ng build --configuration hml",
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
