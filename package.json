{
  "name": "admin",
  "version": "2.4.1",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "homologation": "ng build --configuration hml",
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
