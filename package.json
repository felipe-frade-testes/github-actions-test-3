{
  "name": "admin",
  "version": "1.2.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "homologation": "ng build --configuration hml",
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
